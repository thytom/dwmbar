#!/bin/bash
set -euo pipefail
. "$(dirname "$0")/../lib/common.sh"

# Prints out your public IP address
# Depends on curl

PREFIX=' '
URL=${PUBLIC_IP_URL:-https://ifconfig.co}

get_pub_ip() {
    out=$(safe_curl "$URL" 2 || true)
    echo "$PREFIX$out"
}

get_pub_ip
